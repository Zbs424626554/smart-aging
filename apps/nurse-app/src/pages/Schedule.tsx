import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const Schedule: React.FC = () => {
  return (
    <div>
      <Title level={2}>排班管理</Title>
      <Card>
        <p>排班管理页面，具体功能待开发...</p>
      </Card>
    </div>
  );
};

export default Schedule; 